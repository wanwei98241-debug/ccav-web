"use client";

import { motion } from "framer-motion";
import { ArrowRight, Play, Flame } from "lucide-react";
import Link from "next/link";

export default function HeroSection() {
  return (
    <section className="relative min-h-[90vh] flex items-center justify-center overflow-hidden pt-16">
      {/* Background gradient */}
      <div className="absolute inset-0 bg-gradient-to-br from-[#0d1117] via-[#161b22] to-[#1a2332]" />
      <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,_rgba(88,166,255,0.15)_0%,_transparent_50%)]" />
      <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_bottom_left,_rgba(188,140,255,0.1)_0%,_transparent_50%)]" />

      {/* Floating elements */}
      <motion.div
        animate={{ y: [0, -20, 0] }}
        transition={{ duration: 6, repeat: Infinity, ease: "easeInOut" }}
        className="absolute top-1/4 right-1/4 w-64 h-64 bg-[#58a6ff]/5 rounded-full blur-3xl"
      />
      <motion.div
        animate={{ y: [0, 20, 0] }}
        transition={{ duration: 8, repeat: Infinity, ease: "easeInOut" }}
        className="absolute bottom-1/4 left-1/4 w-48 h-48 bg-[#bc8cff]/5 rounded-full blur-3xl"
      />

      <div className="relative z-10 max-w-4xl mx-auto px-5 text-center">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
        >
          <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-[#58a6ff]/10 border border-[#58a6ff]/20 text-[#58a6ff] text-sm mb-8">
            <span className="w-2 h-2 rounded-full bg-[#58a6ff] animate-pulse" />
            零代码 · 免费API · 对标T/CCPS 0041—2026团标
          </div>
        </motion.div>

        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.1 }}
          className="text-4xl md:text-6xl font-bold text-[#f0f6fc] leading-tight mb-6"
        >
          不写一行代码
          <br className="my-4" />
          <span className="bg-gradient-to-r from-[#58a6ff] via-[#7ec8e3] to-[#bc8cff] bg-clip-text text-transparent">
            把你的文字灵感变成高质感大片
          </span>
        </motion.h1>

        <motion.p
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.2 }}
          className="text-lg md:text-xl text-[#8b949e] max-w-2xl mx-auto mb-8"
        >
          接入免费API → 输入故事 → 一键分镜生图 → 动态渲染 → 智能配乐配音。
          浏览器内完成，零安装，免费额度就能跑通全流程。
        </motion.p>

        {/* 实操流程展示（简版） */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.25 }}
          className="flex flex-wrap items-center justify-center gap-2 mb-10"
        >
          {["接入免费API", "输入故事", "一键分镜生图", "动态渲染", "智能配乐配音"].map((step, i) => (
            <span key={step} className="flex items-center gap-1 px-3 py-1.5 rounded-full bg-[#161b22] border border-[#30363d] text-sm text-[#c9d1d9]">
              {i > 0 && <span className="text-[#30363d] mx-0.5">→</span>}
              <Flame className="w-3 h-3 text-[#d2991d]" />
              {step}
            </span>
          ))}
        </motion.div>

        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.3 }}
          className="flex flex-col sm:flex-row items-center justify-center gap-4"
        >
          <Link
            href="/courses/"
            className="group flex items-center gap-2 px-8 py-4 rounded-xl bg-gradient-to-r from-[#e53e3e] to-[#c53030] text-white font-semibold text-lg hover:shadow-lg hover:shadow-[#e53e3e]/20 transition-all"
          >
            探索课程
            <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
          </Link>
          <Link
            href="/playground/"
            className="flex items-center gap-2 px-8 py-4 rounded-xl bg-[#161b22] border border-[#30363d] text-[#c9d1d9] font-semibold text-lg hover:border-[#bc8cff]/50 hover:text-[#bc8cff] transition-all"
          >
            <Play className="w-5 h-5" />
            AI工坊
          </Link>
        </motion.div>

        {/* Stats */}
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.5 }}
          className="mt-16 grid grid-cols-3 gap-8 max-w-lg mx-auto"
        >
          {[
            { num: "6", label: "团标等级", sub: "国家认证T/CCPS 0041—2026" },
            { num: "5", label: "天师资培训", sub: "从学员到认证讲师，5天跃升" },
            { num: "0", label: "硬件门槛", sub: "浏览器即学即用，零安装零配置" },
          ].map((stat) => (
            <div key={stat.label} className="text-center">
              <div className="text-2xl md:text-3xl font-bold text-[#f0f6fc]">{stat.num}</div>
              <div className="text-sm text-[#8b949e] mt-1">{stat.label}</div>
              <div className="text-xs text-[#484f58]">{stat.sub}</div>
            </div>
          ))}
        </motion.div>
      </div>
    </section>
  );
}
